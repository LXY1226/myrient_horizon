Task 3: Align cmd/server/main.go to worker lifecycle pattern
===========================================================

Date: 2025-03-07
Status: COMPLETED

Changes Applied:
---------------

1. Simplified signal handling (line 29-35):
   - Changed to single buffered signal channel (buffer size 1)
   - One goroutine that logs once and calls cancel()
   - Removed multi-signal escalation logic

2. Reordered initialization - all sync deps before any goroutines:
   - Parse flags (line 17-24)
   - Create ctx/cancel (line 26-27)
   - Install signal handler (line 29-35)
   - Load tree (line 37-43)
   - Connect DB (line 45-52)
   - Build stree.Tree (line 54-58)
   - Create/configure hub with WorkerVersion, WorkerDownloadURL, WorkerSHA256 BEFORE starting hub.Run (line 60-68)
   - Build handlers (line 70-72)
   - Create http.Server (line 73-79)
   - Create listener synchronously (line 81-86)
   - THEN start goroutines (line 90-96)

3. Moved listener creation synchronous (line 81-86):
   - getListener(*addr) happens BEFORE go server.ServeTLS(...)
   - Startup failures happen before background work

4. Cluster goroutine startup (line 90-96):
   - Start hub.Run(ctx) and server.ServeTLS in adjacent goroutines
   - Clear startup structure

5. Fixed serve error handling (line 92-96, 101-104):
   - Replaced log.Fatalf inside serve goroutine
   - Send error to buffered errCh
   - Main select on ctx.Done() or errCh
   - On serve error, log it and cancel() for single shutdown path

6. Simplified shutdown orchestration (line 98-113):
   - After <-ctx.Done() or errCh, create 10s shutdown context
   - Call server.Shutdown(shutdownCtx)
   - Removed extra trailing cancel()
   - Single shutdown tail with final wait point and terminal log

7. Fixed race condition (line 62-64, 91):
   - Set hub fields (WorkerVersion, WorkerDownloadURL, WorkerSHA256) BEFORE go hub.Run(ctx)

Build Verification:
------------------
- cmd/server: COMPILES SUCCESSFULLY
- cmd/verifier: COMPILES SUCCESSFULLY
- cmd/worker: PRE-EXISTING ERRORS (undefined functions - not related to this task)

Pattern Alignment:
-----------------
The server now follows the same lifecycle orchestration shape as the worker:
1. Flag parsing
2. Context creation
3. Signal handler installation
4. Synchronous dependency initialization
5. Synchronous resource creation (listener)
6. Goroutine startup
7. Wait on ctx.Done() or errCh
8. Graceful shutdown with timeout
